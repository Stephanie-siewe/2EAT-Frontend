import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'isj.ing.isi.projet',
  appName: 'GrubHubber',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
